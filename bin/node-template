#!/bin/bash

git clone https://github.com/winton/node-template.git $1

if [ $? -eq 0 ]; then
  cd $1
else
  exit 1
fi

echo ""
npm install

echo ""
grunt setup:branch

echo "\033[1;31mPlease run `grunt setup` to finish setting up your project!\033[m"